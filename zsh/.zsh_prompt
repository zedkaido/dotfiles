#!/usr/bin/env zsh

setopt PROMPT_SUBST

function git_branch() {
	local branch_name=$(git symbolic-ref --short HEAD 2>/dev/null)
	if [ -n "$branch_name" ]; then
		echo "(${branch_name}%f)"
	fi
}

LAMBDA_SYMBOL=$'\λ'
NEWLINE=$'\n'

PROMPT="$(pwd) $(git_branch)${NEWLINE}${LAMBDA_SYMBOL} "
