#!/usr/bin/env bash

# usage: prompt "Do you want to SHUTDOWN?" "sudo shutdown -h now"
if [ -n "$1" ]; then
	[ $(echo "No\nYes\n" | dmenu -i -p "$1") = "Yes" ] && $2
	exit 0
else
	echo "That's life..."
	exit 1
fi
