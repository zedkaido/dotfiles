#!/usr/bin/env bash

monitor="${1:-0}"

find /Applications \
     /System/Applications \
     ~/Applications \
     -name "*.app" -maxdepth 2 ! -name ".*" | dmenu -i -p "OPEN APP" -m $monitor | xargs -I{} open "{}" && exit
