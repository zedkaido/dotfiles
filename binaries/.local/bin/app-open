#!/usr/bin/env bash

monitor="${1:-0}"

xq ; find /Applications \
     /System/Applications \
     ~/Applications \
     -name "*.app" -maxdepth 2 ! -name ".*" | dmenu -i -p "OPEN APP" -m "$monitor" | xargs -I{} open "{}" 
